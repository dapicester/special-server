%a{href: user_path(current_user)}
  = gravatar_for current_user, size: 52
%h1
  = current_user.name
%span
  = link_to t('.view_profile'), current_user
%span
  = t('.microposts', count: current_user.microposts.count)
