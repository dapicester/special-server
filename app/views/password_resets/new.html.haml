- provide :title, 'Reset Password'
%h1= 'Reset Password'

.row
  .span6.offset3
    = form_for(@form) do |f|
      = render 'shared/error_messages', object: f.object
      / TODO User form for password partial
      = f.label :email
      %br/
      = f.text_field :email

      = f.submit 'Reset', class: 'btn btn-large btn-primary'
    %p
      We will send you an email with instruction on how to change you password.
