<li id="fat-menu" class="dropdown">
  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
    <%= flag(I18n.locale) %> <b class="caret"></b>
  </a>
  <ul class="dropdown-menu">
    <% supported_locales.each do |locale, description| %>
      <li><%= link_to flag(locale) + "  " + description, locale: locale %></li>
    <% end %>
  </ul>
</li>
